class Solution {
    static int repeatedSumOfDigits(int N) {
       
         if (N < 10) {
            return N;
        }

        int sum = 0;
        while (N > 0) {
            sum += N % 10;
            N /= 10;
        }

        return repeatedSumOfDigits(sum);
    }
}
